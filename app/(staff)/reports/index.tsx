import { PlaceholderScreen } from '../../../src/ui/PlaceholderScreen';

export default function ReportsScreen() {
  return (
    <PlaceholderScreen
      title="Reports"
      description="Sales, revenue, tax, service, technician, payroll, and inventory — export PDF/Excel/CSV."
    />
  );
}
