import { PlaceholderScreen } from '../../../src/ui/PlaceholderScreen';

export default function EstimatesScreen() {
  return (
    <PlaceholderScreen
      title="Estimates"
      description="Labor, parts, GST (CGST/SGST/IGST), discounts, PDF, and approval via app / SMS / WhatsApp."
    />
  );
}
